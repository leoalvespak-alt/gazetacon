const BubbleMenu = require('@tiptap/extension-bubble-menu');
const FloatingMenu = require('@tiptap/extension-floating-menu');

console.log('Exports from BubbleMenu:', Object.keys(BubbleMenu));
console.log('Exports from FloatingMenu:', Object.keys(FloatingMenu));
